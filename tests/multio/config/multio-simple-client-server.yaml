client:
  on-error: abort-transport
  plans:
    - name: client-plan
      actions:
        - type: transport
          target: server

server:
  step-meter-file: "custom_step_meter_file"
  transport: mpi
  on-error: abort-transport
  plans:
    - name: server-plan
      actions:
        - type: aggregate
        - type: sink
          sinks: []
